<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TimeTracker.Pages.PastebinPage"
             xmlns:pastebin="clr-namespace:TimeTracker.Pastebin"
             Title="PastebinPage"
             x:DataType="pastebin:PastebinPageViewModel">
    <VerticalStackLayout>
        <Button Text="Get Latest paste" Command="{Binding GetLatestPasteCommand}" />
        <Label Text="{Binding Poem}" LineBreakMode="WordWrap" />

        <Button Text="Backup working hours" Command="{Binding CreatePastebinPasteCommand}"/>
        <Label Text="{Binding LastPasteURL}"/>
    </VerticalStackLayout>
</ContentPage>